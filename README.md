
## Цей проект містить серію практичних експериментів для вивчення основ блокової моделі CSS та позиціонування елементів у найніжніших дівочих тонах з котячою тематикою!

##  Опис проекту

Проект складається з кількох HTML-сторінок, кожна з яких демонструє різні аспекти роботи з CSS у пастельних рожевих, лавандових та персикових тонах з котячою тематикою:

1. ** Блокові та інлайнові елементи** - робота з `<div>` та `<span>` - як кіт грається з блоками!
2. ** Normalize.css** - порівняння з normalize.css - як кіт наводить порядок у стилях
3. ** Reset.css** - порівняння з reset.css - кіт скидає все до початку
4. ** CSS властивості** - display та box-sizing - як кіт вибирає свою позицію
5. ** Float та Clear** - обтікання елементів - як кіт обтікає перешкоди
6. ** Overflow** - управління переповненням - кіт контролює контент
7. ** Позиціонування** - static, relative, absolute, fixed, sticky - різні позиції кота

##  Запуск проекту

Просто відкрийте файл `index.html` у вашому браузері або використовуйте локальний сервер:

```bash
# За допомогою Python
python -m http.server 8000

# За допомогою Node.js (http-server)
npx http-server
```

Потім відкрийте в браузері: `http://localhost:8000`

## 📁 Структура проекту

```
pkgmd3/
├── index.html              # Головна сторінка з навігацією
├── block-inline.html       # Блокові та інлайнові елементи
├── normalize.html          # Сторінка з normalize.css
├── reset.html              # Сторінка з reset.css
├── css-properties.html     # Display та box-sizing
├── float-clear.html        # Float та clear
├── overflow.html           # Overflow експерименти
├── positioning.html        # Позиціонування елементів
├── styles/
│   ├── main.css           # Основні стилі у ніжних тонах
│   ├── normalize.css      # Normalize.css
│   ├── reset.css          # Reset.css
│   ├── block-inline.css   # Стилі для блокових/інлайнових
│   ├── comparison.css     # Стилі для порівняння
│   ├── css-properties.css # Стилі для CSS властивостей
│   ├── float-clear.css    # Стилі для float/clear
│   ├── overflow.css       # Стилі для overflow
│   └── positioning.css    # Стилі для позиціонування
└── README.md              # Документація
```


##  Що вивчається

###  Блокові та інлайнові елементи
- Різниця між `<div>` та `<span>` - як кіт грається з блоками
- Властивість `display` та її значення
- Комбінація блокових та інлайнових елементів - великий кіт з кошенятами

###  Normalize.css vs  Reset.css
- Порівняння підходів до нормалізації стилів
- Переваги та недоліки кожного підходу
- Практичне застосування - як кіт наводить порядок

### 🐾 CSS властивості
- `display`: block, inline, inline-block, flex, grid, none - різні позиції кота
- `box-sizing`: content-box vs border-box - як кіт вимірює своє місце
- Вплив на розрахунок розмірів елементів

###  Float та Clear
- Обтікання елементів за допомогою `float` - як кіт обтікає перешкоди
- Контроль обтікання через `clear` - кіт не обтікає!
- Техніка clearfix - коробка з котами

###  Overflow
- `overflow`: visible, hidden, scroll, auto - кіт в коробці
- `overflow-x` та `overflow-y` - кіт контролює свої рухи
- Практичні застосування

###  Позиціонування
- `position`: static, relative, absolute, fixed, sticky - різні позиції кота
- Різниця між `fixed` та `sticky` - кіт завжди на місці vs кіт прилип
- Використання `z-index` - який кіт зверху

##  Технології

- HTML5
- CSS3 (у ніжних дівочих тонах з котячою тематикою)
- Normalize.css
- Reset.css

##  Кольорова палітра

Проект використовує ніжну пастельну палітру:
-  Рожевий: #FFB6C1, #FFC0CB, #FFD1DC
-  Лавандовий: #DDA0DD, #EE82EE
-  Персиковий: #FFE4E1, #FFD1DC
-  М'ятний: #F0FFF0

##  Котяча тематика

Весь проект пронизаний котячою тематикою:
- Кожен елемент має котячі емодзі
- Приклади пояснюються через котячі аналоги
- Навігація та заголовки містять котячі символи
- Кожна сторінка розповідає про CSS через призму котів

